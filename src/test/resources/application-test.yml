# テスト向け設定
env:
  otlp:
    enabled: false # テストはOTELは無効なのを前提にしているためfalse固定すること

spring:
  main:
    banner-mode: off

rms:
  jwt-encode:
    claim:
      issuer: JsonWebTokenIntegrationTest
  remote-stub:
    image: "msa-spring-rms-remote-resources:0.0.1-SNAPSHOT"
